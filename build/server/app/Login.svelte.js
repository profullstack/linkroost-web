/* generated by Svelte v3.59.1 */
import { add_attribute, create_ssr_component } from "svelte/internal";

const Component = create_ssr_component(($$result, $$props, $$bindings, slots) => {
	const user = {};

	async function login(e) {
		e.preventDefault();
		console.log('login user', user);
	}

	return `<form method="post"><fieldset><legend>Login</legend>
      <div class="field"><label for="email">Email</label>
        <input type="email" id="email" name="email"${add_attribute("value", user.email, 0)}></div>
      <div class="field"><label for="password">Password</label>
        <input type="password" name="password" id="password"${add_attribute("value", user.password, 0)}></div></fieldset>
    <footer><button>Login</button>
      <p>Need an account? <a href="/register">Signup</a> or <a href="/forgot">Forgot password?</a></p></footer>
  </form>`;
});

export default Component;